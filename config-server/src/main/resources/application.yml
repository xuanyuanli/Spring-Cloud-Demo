spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/li15038043160/Spring-Cloud-Demo
          searchPaths: respo
          username:
          password:
        label: master

server:
  port: 8888
